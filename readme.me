1.注册机默认选择的是nacos,可进行包的修改
2.对应的maven  jar，例如spring-cloud-alibaba-dependencies 需要从alibaba 的github上拉下来自己打jar发布到本地仓库中，中央仓库中没有该jar包
3.nest 使用的是master版本
4.如果只是进行DEMO演示，则只需要启动nacos,activeMq 和user-context
5.如果要进行spring cloud演示,则把admin-api等启动就可以互相调用